import fs from 'fs'
let handler = async (m, { conn, usedPrefix: _p }) => {
m.reply("SimpleVersi: https://github.com/rasssya76/R-BOT\nFull? Chat Owner price? asked the owner")
}

handler.command = /^(sc)$/i

export default handler